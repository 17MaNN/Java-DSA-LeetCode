Approach

If n is even: use pairs 
(
𝑥
,
−
𝑥
)
(x,−x).

If n is odd: include 0, and use pairs 
(
𝑥
,
−
𝑥
)
(x,−x) for the remaining elements.

All values are unique, and each pair sums to 0, so the total sum is 0.
